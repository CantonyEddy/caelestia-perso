-- user/env.lua — variables d'environnement
-- hl.env("NOM", "valeur") ; pour lire une var existante : os.getenv("XDG_...")

-- hl.env("GTK_THEME", "Adwaita:dark")

return true  -- (require attend une valeur ; évite un warning de module vide)
