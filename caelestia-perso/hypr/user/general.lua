-- user/general.lua — section general (gaps, bordures, layout)
-- La plupart sont pilotés par les variables Caelestia (voir hypr-vars.lua).

-- hl.config({ general = { gaps_in = 4, gaps_out = 8 } })

return true
