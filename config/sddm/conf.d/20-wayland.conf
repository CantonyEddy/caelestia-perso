# 20-wayland.conf — compositor du greeter Wayland
# Déployé dans /etc/sddm.conf.d/ par install.sh (copie via sudo).
[Wayland]
CompositorCommand=start-hyprland -- --config /etc/sddm/hyprland-greeter.conf
