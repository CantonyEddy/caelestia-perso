-- user/execs.lua — lancement au démarrage
-- En Lua : hl.dispatch(hl.dsp.exec_cmd("...")) ou hl.exec_once("...") selon version.
-- Le plus portable reste le dispatcher exec.

-- hl.dispatch(hl.dsp.exec_cmd("nm-applet"))

return true
